site.url=${site.url}
api.url=${api.url}
site.basepath=${site.basepath}
site.port=${site.port}
browser=${browser}
platform=${platform}
testToRun=${testToRun}
driver.root.dir=${driver.root.dir}

#Database properties
jdbcUrl = ${jdbcUrl}
jdbcDriver =${jdbcDriver}
jdbcUser = ${jdbcUser}
jdbcPwd = ${jdbcPwd}
